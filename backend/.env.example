# For local dev without Postgres installed, just leave DATABASE_URL commented out —
# app/core/config.py defaults to a local SQLite file (bloodbank.db).
#
# When you're ready to use Postgres (recommended once your teammate is on board too):
# DATABASE_URL=postgresql://user:password@localhost:5432/bloodbank

SECRET_KEY=This-is-my-secret-bro
ALGORITHM=HS256

